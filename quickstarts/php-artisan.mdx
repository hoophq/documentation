---
title: "PHP Artisan"
description: "Interact with the Artisan console or execute ad-hoc scripts."
---

import Prerequisites from '/snippets/QuickstartsPrerequisites.mdx';

<Prerequisites />

## Configuration

To proxy the `php artisan tinker` command, execute the following command.

```bash
hoop run --name artisan-demo --command 'php /path/to/your/app artisan tinker'
```

<Info>
If the command returns an exit code 1 when interacting via tinker.
Configure the PsySH by disabling pcnl support:

    <AccordionGroup>
        <Accordion title="Configure Psysh">
            ```sh
            cat - > ~/.config/psysh/config.php <<EOF
            <?php
            return [
                'usePcntl' => false,
            ];
            EOF
            ```
        </Accordion>
    </AccordionGroup>
</Info>



## How to Use

Users can now connect and perform ad-hoc executions via Webapp.

```bash
hoop connect artisan-demo
```

