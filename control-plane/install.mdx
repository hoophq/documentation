---
title: "Overview"
description: "Deploy the Control Plane in your own infrastructure with Docker Compose, Kubernetes or AWS."
---

The [Control Plane](/core-concepts/control-plane) is the service your Sidecars register with and pull configuration from. You run one per environment.

<Note>
  The Control Plane is an **Enterprise** feature and requires a license. [Talk to us](https://hoop.dev/start) to get one, or use the [managed service](https://use.hoop.dev) and skip this page entirely.
</Note>

---

## Before you start

- A license key.
- A hostname your Sidecars can reach over HTTPS. Sidecars only ever make outbound requests, so the Control Plane does not need to reach *them*.
- A PostgreSQL database. The compose file below brings one up; point `POSTGRES_DB_URI` at your own instead if you have one.

---

## Deployment options

The mode is chosen by the subcommand: `hoop start control-plane` rather than
`hoop start gateway`. One image serves both.

<CardGroup cols={3}>
  <Card title="Docker Compose" icon="docker" href="/control-plane/deployment/docker-compose">
    The supported path today. Good for evaluation and for single-host production.
  </Card>
  <Card title="Kubernetes" icon="dharmachakra" href="/control-plane/deployment/kubernetes">
    Deploys the gateway. The chart has no value for control plane mode yet.
  </Card>
  <Card title="AWS" icon="aws" href="/control-plane/deployment/aws">
    Deploys the gateway. The CloudFormation template has no parameter for the mode yet.
  </Card>
</CardGroup>

---

## After it is running

<Steps>
  <Step title="Create the first account">
    Authentication is local by default — the Control Plane manages users and passwords itself, so you can sign in as soon as it is up.
  </Step>
  <Step title="Apply your license">
    See [License Management](/setup/license-management).
  </Step>
  <Step title="Connect your first Sidecar">
    Issue a token, point a Sidecar at the Control Plane, and confirm it picked up its configuration. See [Connect a Sidecar](/control-plane/connect-sidecar).
  </Step>
</Steps>

---

## Keeping it current

See [Updating](/setup/updating) for the upgrade process. Sidecars and the Control Plane are versioned independently, but keep them within a minor release of each other.

---

## Next

<CardGroup cols={2}>
  <Card title="Connect a Sidecar" icon="link" href="/control-plane/connect-sidecar">
    Token, server host, and confirming the handshake.
  </Card>
  <Card title="Environment Variables" icon="list" href="/setup/configuration/env-vars">
    Every variable the service reads.
  </Card>
</CardGroup>
