---
title: "1.42.1"
description: "Fixed a critical bug where HTTP proxy environment variables and headers couldn't be accessed."
---

### TL;DR
- **Bug Fix:** HTTP proxy environment variables and headers now work correctly

---

### Bug Fix

**HTTP Proxy Environment Variables ([#1073](https://github.com/hoophq/hoop/pull/1073))**

Fixed a bug where environment variables and headers couldn't be accessed in HTTP proxy connections. The issue blocked index-based operations on environment variables due to a data structure problem.

**What changed:**
- Environment variables now initialize correctly as vectors
- Headers processing properly filters the `INSECURE` variable
- Improved error handling when environment-variables collection is empty or nil

---

Small fix. Big unblock.

