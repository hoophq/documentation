---
title: "1.42.9"
description: "Data masking for MongoDB, RDP improvements, and a crucial fix for connection form persistence. This one's all about reliability and security."
---

### TL;DR
- **New:** Microsoft Presidio data masking for MongoDB
- **Enhanced:** RDP connection architecture
- **Fixed:** Stale data persisting when switching between connections
- **Improved:** Connection metadata download error handling

---

### New Features

**MongoDB Data Masking with MS Presidio**

Microsoft Presidio now works with MongoDB connections. Data masking for MongoDB aligns with the same architecture used across all databases.

---

### Enhancements

**RDP Connection Architecture ([#1084](https://github.com/hoophq/hoop/pull/1084))**

RDP connections are now configured as custom type with `rdp` subtype, providing more flexibility and consistency with other connection types.

**Connection Metadata Script ([#1099](https://github.com/hoophq/hoop/pull/1099))**

Improved the connection metadata download script with better HTTP status handling and JSON validation. Clear error messages now show for 403, 404, and 429 responses.

---

### Bug Fixes

**Connection Form State Persistence ([#1100](https://github.com/hoophq/hoop/pull/1100))**

Fixed a bug where switching from one connection to another in the terminal retained data from the first. Connection details now load synchronously, so each connection starts with a clean state.

If you noticed old data appearing in connection forms, this fixes it.

---

MongoDB gets proper data masking. Connection management gets a cleaner state.

