---
title: "IAM Federation for GCP"
description: "Mint per-user OAuth tokens for BigQuery and other GCP resources instead of sharing a single service-account key."
---

## What this gives you

With IAM Federation enabled, Hoop stops sharing a single Google service-account key with every user. Instead, each user's activity against GCP is attributed to **their own principal** — so a `bq query` Alice runs through Hoop shows up in Cloud Logging as Alice, not as a shared admin service account.

This buys you:

- **Per-user attribution** — every GCP operation is tied to the individual who ran it
- **No shared keys** — users never hold a long-lived service-account key; Hoop mints short-lived tokens at session time
- **A clean audit trail** — GCP records *who* ran each query (and, for `gcp_iam`, *through whom*)

## Two ways to federate

Hoop ships two built-in GCP providers. They achieve the same goal — per-user attribution with no shared static key — but differ in what you provision on the GCP side and whose identity ends up in the audit log.

| | `gcp_iam` | `gcp_oauth` |
|---|---|---|
| **Identity in GCP audit log** | A per-user **service account** (e.g. `alice@proj.iam.gserviceaccount.com`) | The user's **real Google account** (e.g. `alice@acme.com`) |
| **What you provision** | One admin SA + one SA per user (or a shared SA) | One OAuth **client** (app registration) — **zero service accounts** |
| **Admin credential Hoop stores** | Admin SA JSON key | OAuth `client_id` / `client_secret` |
| **How a token is minted** | Admin SA impersonates the user SA via `iamcredentials.GenerateAccessToken` | Each user consents once; Hoop exchanges their **refresh token** for an access token |
| **Per-user setup** | Admin grants `serviceAccountTokenCreator` + data roles to each SA | Each user clicks **Connect Google account** once; data roles go on their real Google identity |
| **Web UI support** | Configurable in the connection form | Configurable in the connection form; each user clicks **Connect Google account** in the web client |

<Tip>
  Choose **`gcp_iam`** when you want Hoop-managed service-account identities and
  a delegation chain proving every query went *through* Hoop. Choose
  **`gcp_oauth`** when you want the GCP audit log to show the **actual human's
  email** and prefer to provision **no service accounts at all**.
</Tip>

## How It Works

When a user runs `hoop connect my-bq`, the gateway reads the connection's federation policy and mints a short-lived token for that user, then hands it to the agent for the session. The minting step depends on the provider:

<Tabs>
  <Tab title="gcp_iam (service-account impersonation)">
    <Steps>
      <Step title="Reads the federation policy">
        The policy defines the identity template, project id, and fallback rules that map the Hoop user to a GCP service account.
      </Step>
      <Step title="Mints a token as the user's principal">
        The gateway asks Google's `iamcredentials` API to mint a short-lived OAuth access token **as the user's target service account**, using the admin service account stored for your organization.
      </Step>
      <Step title="Hands the token to the agent">
        The agent uses that short-lived token for the session. Every subsequent operation is attributed to the user's service account — not the shared admin account.
      </Step>
    </Steps>

    For this to work, two things must be true on the GCP side:

    - The **admin service account** must be allowed to impersonate **each user's target principal** (the `roles/iam.serviceAccountTokenCreator` grant).
    - Each **target principal** must have whatever data-plane roles it needs (e.g. `roles/bigquery.user` for BigQuery).
  </Tab>
  <Tab title="gcp_oauth (per-user OAuth)">
    <Steps>
      <Step title="Each user consents once">
        The user opens the connection's consent URL and approves Google's OAuth screen with their own corporate Google account. Hoop stores their **refresh token** encrypted at rest, keyed to that user and connection.
      </Step>
      <Step title="Mints a token as the user">
        At session-open the gateway exchanges that refresh token for a fresh, short-lived **user** access token. No service account is involved.
      </Step>
      <Step title="Hands the token to the agent">
        The agent uses the token for the session. Every operation is attributed to the user's real Google identity.
      </Step>
    </Steps>

    For this to work on the GCP side:

    - You register **one OAuth client** (Web application) — no service accounts.
    - Each user's **own Google account** must hold the data-plane roles it needs (e.g. `roles/bigquery.user`).
  </Tab>
</Tabs>

## Configure per-user OAuth (`gcp_oauth`)

This walkthrough sets up the no-service-account mode. For the service-account
(`gcp_iam`) setup — admin SA, Token Creator grants, and the connection form —
see the [configuration guide](/setup/configuration/iam-federation-gcp-configuration).

<Note>
  `gcp_oauth` is fully supported in the web UI: an admin configures it on the
  connection form (Step 3), and each user connects their own Google account from
  the web client (Step 4). The equivalent `hoop admin federation` CLI flow is
  shown alongside each step for automation.
</Note>

### Step 1 — Create an OAuth client in GCP (no service accounts)

In the [GCP Console](https://console.cloud.google.com/) for the project that
holds your BigQuery data:

<Steps>
  <Step title="Open Credentials → Create credentials">
    Go to **APIs & Services → Credentials** and click **+ Create credentials**.

    <Frame>
      <img src="/images/configure/iam-federation/gcp-oauth/01-credentials-create.png" alt="GCP APIs & Services Credentials page with the Create credentials button highlighted" />
    </Frame>
  </Step>
  <Step title="Choose OAuth client ID">
    From the dropdown, pick **OAuth client ID** (not API key) — this is what
    requests user consent so Hoop can act as each user.

    <Frame>
      <img src="/images/configure/iam-federation/gcp-oauth/02-create-oauth-client-id.png" alt="Create credentials dropdown showing API key and OAuth client ID options, with OAuth client ID selected" />
    </Frame>
  </Step>
  <Step title="Configure the client and add the redirect URI">
    Set **Application type** to **Web application**, give it a name (e.g.
    `OAuth Bq federation connection`), and under **Authorized redirect URIs**
    add your gateway's API URL plus `/api/federation/oauth/callback`:

    ```
    https://GATEWAYURI.com/api/federation/oauth/callback
    ```

    Replace `GATEWAYURI.com` with your gateway host (local dev:
    `http://localhost:8009/api/federation/oauth/callback`). The URI must match
    **exactly**. Click **Create**.

    <Frame>
      <img src="/images/configure/iam-federation/gcp-oauth/03-oauth-client-form.png" alt="Create OAuth client ID form with Web application type, a name, and the authorized redirect URI pointing at /api/federation/oauth/callback" />
    </Frame>
  </Step>
  <Step title="Configure the consent screen scopes">
    On the **OAuth consent screen**, add the scope
    `https://www.googleapis.com/auth/cloud-platform` (or a narrower data scope,
    e.g. BigQuery). Add your users as test users, or publish the app.
  </Step>
  <Step title="Copy the Client ID and Client secret (or download the JSON)">
    After creating the client, Google shows the **Client ID** and **Client
    secret**. Copy them, or click **Download JSON**.

    <Frame>
      <img src="/images/configure/iam-federation/gcp-oauth/04-oauth-client-created.png" alt="OAuth client created dialog showing the Client ID, Client secret, and a Download JSON button" />
    </Frame>
  </Step>
</Steps>

The downloaded file wraps everything under a `"web"` key with extra fields Hoop
doesn't need. Hoop only needs the **client_id** and **client_secret** — pull
those two values into a minimal JSON object:

```json
{
  "client_id": "968792117476-0nom9m8e8...apps.googleusercontent.com",
  "client_secret": "GOCSPX-kdf3IWCfH-..."
}
```

You'll paste this in Step 3 (or save it as `oauth-client.json` for the CLI flow).

<Warning>
  While the consent screen is in **Testing** mode, Google expires refresh
  tokens after **7 days**, so federated sessions break weekly until each user
  re-consents. **Publish the app** to remove that cap.
</Warning>

### Step 2 — Grant data roles to each user's Google account

There are no service accounts to grant. Give each user's **real Google
identity** the data-plane roles it needs — for BigQuery:

```bash
export PROJECT_ID="my-proj"

gcloud projects add-iam-policy-binding "$PROJECT_ID" \
  --member="user:alice@acme.com" \
  --role="roles/bigquery.user"

gcloud projects add-iam-policy-binding "$PROJECT_ID" \
  --member="user:alice@acme.com" \
  --role="roles/bigquery.dataViewer"
```

### Step 3 — Wire federation onto the connection (admin, one-time)

<Tabs>
  <Tab title="Web UI">
    Open the BigQuery connection (or create one), and under **Role connection
    method** choose **IAM Federation**. Then in **Federation setup**:

    <Steps>
      <Step title="Select the GCP OAuth provider">
        Set **Federation provider** to **GCP OAuth (per-user Google account)**.

        <Frame>
          <img src="/images/configure/iam-federation/gcp-oauth/05-hoop-select-gcp-oauth.png" alt="Hoop connection form with IAM Federation selected and the Federation provider dropdown open, showing GCP OAuth (per-user Google account)" />
        </Frame>
      </Step>
      <Step title="Paste the OAuth client credentials">
        In **OAuth client credentials (JSON)**, paste the minimal
        `client_id` / `client_secret` JSON from Step 1:

        ```json
        {
          "client_id": "968792117476-0nom9m8e8...apps.googleusercontent.com",
          "client_secret": "GOCSPX-kdf3IWCfH-..."
        }
        ```

        The client secret is only used by the gateway to exchange tokens — it
        never reaches end-user sessions.
      </Step>
      <Step title="Set the project and save">
        Enter your **GCP Project ID** (e.g. `my-proj`) and **Save** the
        connection. Selecting `gcp_oauth` forces the fallback method to **Deny
        the session**, so a session never silently runs without a per-user
        token.
      </Step>
    </Steps>
  </Tab>
  <Tab title="CLI">
    Write a policy file selecting the `gcp_oauth` provider:

    ```yaml
    # federation-oauth.yaml
    builtin_provider: gcp_oauth
    fallback_policy: deny          # or "static" to fall back to the connection's existing envs
    token_ttl_seconds: 3600        # informational; Google controls the real token lifetime
    extra_config:
      project_id: my-proj
      # optional — defaults to cloud-platform:
      # scopes:
      #   - https://www.googleapis.com/auth/bigquery
    ```

    Apply it, passing the **OAuth client JSON** from Step 1 as the admin
    credential (it replaces the service-account key that `gcp_iam` uses):

    ```bash
    hoop admin federation set my-bq \
      --file federation-oauth.yaml \
      --credentials-file ./oauth-client.json

    hoop admin federation get my-bq   # verify: Provider: gcp_oauth, Has Admin Credentials: yes
    ```
  </Tab>
</Tabs>

### Step 4 — Each user connects their Google account (one-time)

Each user grants Hoop a refresh token once, by approving the Google consent
screen **with the corporate Google account that should be attributed**.

<Tabs>
  <Tab title="Web client">
    When a user opens the federated connection in the web client and isn't
    connected yet, a **Connect your Google account** prompt appears in the
    editor. (If they run a query first, the run is blocked and the same
    **Connect Google account** action is shown inline.) Clicking it redirects to
    Google's consent screen and returns to the same page on approval —
    Hoop shows a "Google account connected" confirmation and the prompt
    disappears.

    Admins can also connect/disconnect from the connection's **Federation
    setup** section in the connection form.
  </Tab>
  <Tab title="CLI / API">
    Ask Hoop for the consent URL (this uses the user's own Hoop token, not an
    admin token), then open it in a browser:

    ```bash
    # The user's Hoop access token (from `hoop login`)
    TOKEN="<hoop_access_token>"

    curl -s -H "Authorization: Bearer $TOKEN" \
      "https://hoop.your-company.com/api/connections/my-bq/federation/oauth/authorize"
    # → {"url":"https://accounts.google.com/o/oauth2/auth?...&state=..."}
    ```

    Open the returned `url`, approve consent, and Google redirects back to the
    gateway, which stores the encrypted refresh token and returns the browser to
    the app with `?federation_oauth=success`.
  </Tab>
</Tabs>

<Warning>
  Approve the consent screen with the **same identity you use in Hoop**. Hoop
  records the consented Google account as the session's principal, so it is
  what shows up in every BigQuery audit entry — connecting a different Google
  account would misattribute that user's activity.
</Warning>

To revoke (disconnect the Google account for a connection), use the
**Disconnect** action in the web client, or the API:

```bash
curl -s -X DELETE -H "Authorization: Bearer $TOKEN" \
  "https://hoop.your-company.com/api/connections/my-bq/federation/oauth"
```

Once connected, `hoop connect my-bq` mints a fresh user token per session and
injects `HOOP_GCP_ACCESS_TOKEN`, `CLOUDSDK_CORE_PROJECT`, and
`HOOP_FEDERATED_PRINCIPAL` (and strips any legacy
`GOOGLE_APPLICATION_CREDENTIALS`).

<Note>
  **Test as user** does not apply to `gcp_oauth` (the connection form hides it
  for this provider): a dry-run can't consent on the user's behalf, so there is
  nothing to validate ahead of time. Validate the setup by running a real
  session after Step 4 instead.
</Note>

## The Audit Trail

Both providers attribute every operation to the individual, but the shape of
the audit record differs.

**`gcp_iam`** records a `serviceAccountDelegationInfo` chain showing **who** ran
the query (the user's service account) and **through whom** (the Hoop admin SA):

```yaml
protoPayload.authenticationInfo.principalEmail: alice@my-proj.iam.gserviceaccount.com
protoPayload.authenticationInfo.serviceAccountDelegationInfo:
  - firstPartyPrincipal:
      principalEmail: hoop-admin@my-proj.iam.gserviceaccount.com
```

That chain lets you prove every query in a project went through Hoop and flag
any that bypassed it. Enabling, querying, and sinking these logs is covered in
the configuration guide.

**`gcp_oauth`** records the user's **real Google account** directly as the
principal, with no delegation chain — the human *is* the caller:

```yaml
protoPayload.authenticationInfo.principalEmail: alice@acme.com
```

---

<Note>
Ready to set it up? For `gcp_iam`, the [IAM Federation for GCP configuration guide](/setup/configuration/iam-federation-gcp-configuration) walks through the GCP IAM setup (admin service account, Token Creator grants, verification), wiring the policy onto a connection, and enabling the audit logs. For `gcp_oauth`, follow [Configure per-user OAuth](#configure-per-user-oauth-gcp-oauth) above.
</Note>

## Next Steps

<CardGroup cols={2}>
  <Card title="Configuration Guide" icon="gear" href="/setup/configuration/iam-federation-gcp-configuration">
    Grant Token Creator and wire federation onto a connection
  </Card>
  <Card title="CLI Reference" icon="terminal" href="/clients/cli-federation">
    `hoop admin federation set` and `test` flag reference
  </Card>
  <Card title="Connect to BigQuery" icon="google" href="/quickstart/databases/bigquery">
    Create the connection federation attaches to
  </Card>
  <Card title="Session Recording" icon="film" href="/learn/features/session-recording">
    Audit every federated session inside Hoop
  </Card>
</CardGroup>
